# High-level write-throughput benchmarks


# these are common and can be overridden suite-wide
#
# note for bench_*_many, file size defaults to CHUNK, and SIZE = sum of
# all files
#
defines.SIZE = '1024*1024' # 1 MiB
defines.CHUNK = 64
defines.SEED = 42

# simulate this number of mounts
defines.SIM_COUNT = 10000
# simulated time, in nanoseconds, to run the test
defines.SIM_TIME = 0 # '60ULL*60ULL*1000ULL*1000ULL*1000ULL' # 1 hour
# simulation size in bytes
defines.SIM_SIZE = 0

# set this to true to skip bench warmup
defines.SKIP_WARMUP = false

# run with simulated powerloss? defaults to a nice shutdown
defines.POWERLOSS = false

# force powerloss to occur during rename?
defines.POWERLOSS_RENAME = false

# also bench read-only mount?
defines.ROMOUNT = true

# how long before powerloss/shutdown, we choose a uniform number of
# nanoseconds between these
defines.RUN_MIN = 0
defines.RUN_MAX = '10ULL*1000ULL*1000ULL*1000ULL' # 10 seconds

# spiffs is limited to 32-bit disks
if = 'BENCH_IFDEF_SPIFFS(DISK_SIZE < 0x100000000ULL, true)'

#hooks.read = 'bench_mount_bd_read'
#hooks.prog = 'bench_mount_bd_prog'
#hooks.erase = 'bench_mount_bd_erase'
code = '''
    // include common bench helpers
    #include "benches/bench_helpers.h"
//#
//#    // gc time measurement
//#    bench_sns_t bench_mount_gc_start_time = -1;
//#    // too many issues with yaffs2 and thinking all errors are bad
//#    // blocks, so just use setjmp to abort gc
//#    #ifdef YAFFS2
//#    #include <setjmp.h>
//#    jmp_buf bench_mount_gc_jmp;
//#    #endif
//#
//#    void bench_mount_gc_start(void) {
//#        bench_mount_gc_start_time = BENCH_SIMTIME();
//#    }
//#
//#    void bench_mount_gc_stop(void) {
//#        bench_mount_gc_start_time = -1;
//#    }
//#
//#    // hooks for optional gc time limit
//#    int bench_mount_bd_read(const struct lfs3_cfg *cfg, lfs3_block_t block,
//#            lfs3_off_t off, void *buffer, lfs3_size_t size) {
//#        // gc time limit?
//#        if (GC_TIME && GC_INTERRUPT && bench_mount_gc_start_time != -1) {
//#            // predict how long this operation would take
//#            bench_ns_t optime = READ_TIMING
//#                    + ((lfs3_alignup(off + size, lfs3_max(READ_WIDTH, 1))
//#                            - lfs3_aligndown(off, lfs3_max(READ_WIDTH, 1)))
//#                        * READ_WTIMING)
//#                    + (size * READ_UTIMING);
//#            // would this take too long? abort
//#            if (BENCH_SIMTIME() + optime
//#                    > (bench_ns_t)(bench_mount_gc_start_time + GC_TIME)) {
//#                #ifdef YAFFS2
//#                longjmp(bench_mount_gc_jmp, 1);
//#                #else
//#                return LFS3_ERR_IO;
//#                #endif
//#            }
//#        }
//#
//#        return BENCH_BD_READNOHOOKS(cfg, block, off, buffer, size);
//#    }
//#
//#    int bench_mount_bd_prog(const struct lfs3_cfg *cfg, lfs3_block_t block,
//#            lfs3_off_t off, const void *buffer, lfs3_size_t size) {
//#        // gc time limit?
//#        if (GC_TIME && GC_INTERRUPT && bench_mount_gc_start_time != -1) {
//#            // predict how long this operation would take
//#            bench_ns_t optime = PROG_TIMING
//#                    + ((lfs3_alignup(off + size, lfs3_max(PROG_WIDTH, 1))
//#                            - lfs3_aligndown(off, lfs3_max(PROG_WIDTH, 1)))
//#                        * PROG_WTIMING)
//#                    + (size * PROG_UTIMING);
//#            // would this take too long? abort
//#            if (BENCH_SIMTIME() + optime
//#                    > (bench_ns_t)(bench_mount_gc_start_time + GC_TIME)) {
//#                #ifdef YAFFS2
//#                longjmp(bench_mount_gc_jmp, 1);
//#                #else
//#                return LFS3_ERR_IO;
//#                #endif
//#            }
//#        }
//#
//#        return BENCH_BD_PROGNOHOOKS(cfg, block, off, buffer, size);
//#    }
//#
//#    int bench_mount_bd_erase(const struct lfs3_cfg *cfg, lfs3_block_t block) {
//#        // gc time limit?
//#        if (GC_TIME && GC_INTERRUPT && bench_mount_gc_start_time != -1) {
//#            // predict how long this operation would take
//#            bench_ns_t optime = ERASE_TIMING
//#                    + (lfs3_alignup(BLOCK_SIZE, lfs3_max(ERASE_WIDTH, 1))
//#                        * ERASE_WTIMING)
//#                    + (BLOCK_SIZE * ERASE_UTIMING);
//#            // would this take too long? abort
//#            if (BENCH_SIMTIME() + optime
//#                    > (bench_ns_t)(bench_mount_gc_start_time + GC_TIME)) {
//#                #ifdef YAFFS2
//#                longjmp(bench_mount_gc_jmp, 1);
//#                #else
//#                return LFS3_ERR_IO;
//#                #endif
//#            }
//#        }
//#
//#        return BENCH_BD_ERASENOHOOKS(cfg, block);
//#    }
'''


## sequential write throughput
#[cases.bench_mount_seq]
#code = '''
#    #if defined(LFS3)
#    lfs3_t lfs3;
#    lfs3_format(&lfs3, LFS3_F_RDWR, CFG) => 0;
#    lfs3_mount(&lfs3, LFS3_M_RDWR | LFS3_M_MKCONSISTENT, CFG) => 0;
#    #elif defined(LFS2)
#    lfs2_t lfs2;
#    lfs2_format(&lfs2, CFG_LFS2) => 0;
#    lfs2_mount(&lfs2, CFG_LFS2) => 0;
#    lfs2_fs_mkconsistent(&lfs2) => 0;
#    #elif defined(SPIFFS)
#    spiffs spiffs;
#    spiffs.user_data = CFG;
#    uint8_t *work_buffer = malloc(2*SPAGE_SIZE);
#    uint8_t *fd_buffer = malloc(FD_SIZE);
#    uint8_t *scache_buffer = malloc(SCACHE_SIZE);
#    // spiffs format without magic just needs a disk erase
#    SPIFFS_mount(&spiffs, (spiffs_config*)CFG_SPIFFS, work_buffer,
#            fd_buffer, FD_SIZE,
#            scache_buffer, SCACHE_SIZE,
#            NULL) => 0;
#    #elif defined(YAFFS2)
#    // note this implicitly zeros yaffs_dev
#    struct yaffs_dev yaffs2 = {
#        .driver_context = CFG,
#        .param = CFG_YAFFS2->param,
#        .drv = CFG_YAFFS2->drv,
#    };
#    yaffs_add_device(&yaffs2);
#    yaffs_format("/", false, false, false) => 0;
#    yaffs_mount("/") => 0;
#    #endif
#    if (!SKIP_WARMUP) {
#        BENCH_STACK_PAUSE();
#        BENCH_HEAP_PAUSE();
#        #if defined(LFS3)
#        int err = bench_helpers_warmup(CFG, &lfs3);
#        #elif defined(LFS2)
#        int err = bench_helpers_warmup(CFG, &lfs2);
#        #elif defined(SPIFFS)
#        int err = bench_helpers_warmup(CFG, &spiffs);
#        #elif defined(YAFFS2)
#        int err = bench_helpers_warmup(CFG, &yaffs2);
#        #endif
#        if (err) {
#            LFS3_ERROR("Bench warmup failed: %d", err);
#            return;
#        }
#        BENCH_HEAP_RESUME();
#        BENCH_STACK_RESUME();
#    }
#    uint32_t prng = SEED;
#
#    // reset our timer
#    BENCH_SIMRESET();
#
#    // open a file
#    #if defined(LFS3)
#    lfs3_file_t file;
#    lfs3_file_open(&lfs3, &file, "bench_seq",
#            LFS3_O_WRONLY | LFS3_O_CREAT | LFS3_O_EXCL) => 0;
#    #elif defined(LFS2)
#    lfs2_file_t file;
#    lfs2_file_open(&lfs2, &file, "bench_seq",
#            LFS2_O_WRONLY | LFS2_O_CREAT | LFS2_O_EXCL) => 0;
#    #elif defined(SPIFFS)
#    spiffs_file fd = SPIFFS_open(&spiffs, "bench_seq",
#            SPIFFS_WRONLY | SPIFFS_CREAT | SPIFFS_EXCL, 0777);
#    assert(fd >= 0);
#    #elif defined(YAFFS2)
#    int fd = yaffs_open("bench_seq", O_WRONLY | O_CREAT | O_EXCL, 0777);
#    assert(fd >= 0);
#    #endif
#    lfs3_off_t size = 0;
#    uint64_t written = 0;
#    // ok, one of these needs to be non-zero
#    LFS3_ASSERT(SIM_TIME > 0 || SIM_SIZE > 0);
#    while (!(SIM_SIZE && written >= (uint64_t)SIM_SIZE)
#            && !(SIM_TIME && BENCH_SIMTIME() >= SIM_TIME)) {
#        BENCH_START("write");
#        // arguably we should just rewind and continue writing to the
#        // front of the file when we hit the end, but this overly
#        // penalizes littlefs2, so instead we truncate
#        if (size >= SIZE) {
#            #if defined(LFS3)
#            lfs3_file_rewind(&lfs3, &file) => 0;
#            lfs3_file_truncate(&lfs3, &file, 0) => 0;
#            #elif defined(LFS2)
#            lfs2_file_rewind(&lfs2, &file) => 0;
#            lfs2_file_truncate(&lfs2, &file, 0) => 0;
#            #elif defined(SPIFFS)
#            SPIFFS_lseek(&spiffs, fd, 0, SPIFFS_SEEK_SET) => 0;
#            SPIFFS_ftruncate(&spiffs, fd, 0) => 0;
#            #elif defined(YAFFS2)
#            yaffs_lseek(fd, 0, SEEK_SET) => 0;
#            yaffs_ftruncate(fd, 0) => 0;
#            #endif
#            size = 0;
#        }
#
#        // write to the file
#        uint8_t wbuf[CHUNK];
#        for (lfs3_size_t j = 0; j < CHUNK; j++) {
#            wbuf[j] = 'a' + (BENCH_PRNG(&prng) % 26);
#        }
#        #if defined(LFS3)
#        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
#        #elif defined(LFS2)
#        lfs2_file_write(&lfs2, &file, wbuf, CHUNK) => CHUNK;
#        #elif defined(SPIFFS)
#        SPIFFS_write(&spiffs, fd, wbuf, CHUNK) => CHUNK;
#        #elif defined(YAFFS2)
#        yaffs_write(fd, wbuf, CHUNK) => CHUNK;
#        #endif
#
#        size += CHUNK;
#        written += CHUNK;
#        BENCH_STOP("write", written);
#
#        // run gc
#        if (GC) {
#            BENCH_START("gc");
#            bench_ns_t gc_start = BENCH_SIMTIME();
#            #if defined(LFS3)
#            lfs3_gc_t gc;
#            lfs3_gc_open(&lfs3, &gc, GC_FLAGS) => 0;
#            bench_mount_gc_start();
#            while (!GC_TIME
#                    || BENCH_SIMTIME() - gc_start < (bench_ns_t)GC_TIME) {
#                lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
#                assert(steps >= 0 || steps == LFS3_ERR_IO);
#                if (steps == 0) {
#                    break;
#                }
#            }
#            bench_mount_gc_stop();
#            lfs3_gc_close(&lfs3, &gc) => 0;
#            #elif defined(LFS2)
#            bench_mount_gc_start();
#            if (!GC_TIME
#                    || BENCH_SIMTIME() - gc_start < (bench_ns_t)GC_TIME) {
#                int err = lfs2_fs_gc(&lfs2);
#                assert(!err || err == LFS3_ERR_IO);
#            }
#            bench_mount_gc_stop();
#            #elif defined(SPIFFS)
#            bench_mount_gc_start();
#            for (lfs3_block_t i = 0;
#                    i < GC_RETRY_COUNT
#                        && (!GC_TIME
#                            || BENCH_SIMTIME() - gc_start
#                                < (bench_ns_t)GC_TIME);
#                    i++) {
#                int err = SPIFFS_gc(&spiffs, GC_CLEAN_SIZE);
#                assert(!err || err == LFS3_ERR_IO);
#            }
#            bench_mount_gc_stop();
#            #elif defined(YAFFS2)
#            bench_mount_gc_start();
#            if (!GC_TIME
#                    || BENCH_SIMTIME() - gc_start < (bench_ns_t)GC_TIME) {
#                if (!setjmp(bench_mount_gc_jmp)) {
#                    yaffs_do_background_gc("/", 0);
#                }
#            }
#            bench_mount_gc_stop();
#            #endif
#            BENCH_STOP("gc", written);
#        }
#    }
#    #if defined(LFS3)
#    lfs3_file_close(&lfs3, &file) => 0;
#    #elif defined(LFS2)
#    lfs2_file_close(&lfs2, &file) => 0;
#    #elif defined(SPIFFS)
#    SPIFFS_close(&spiffs, fd) => 0;
#    #elif defined(YAFFS2)
#    yaffs_close(fd) => 0;
#    #endif
#
#    // report the total stack/heap usage after the benchmark
#    #ifdef BENCH_STACK
#    BENCH_RESULT("stack", written, BENCH_STACK_WATERMARK());
#    #endif
#    #ifdef BENCH_HEAP
#    BENCH_RESULT("heap", written, BENCH_HEAP_WATERMARK());
#    #endif
#
#    // find the total disk usage after the benchmark
#    //
#    // note this is garbage because of the above truncates!
#    #if defined(LFS3)
#    uintmax_t usage = bench_helpers_usage(CFG, &lfs3);
#    #elif defined(LFS2)
#    uintmax_t usage = bench_helpers_usage(CFG, &lfs2);
#    #elif defined(SPIFFS)
#    uintmax_t usage = bench_helpers_usage(CFG, &spiffs);
#    #elif defined(YAFFS2)
#    uintmax_t usage = bench_helpers_usage(CFG, &yaffs2);
#    #endif
#    BENCH_RESULT("usage", written, usage);
#
#    #if defined(LFS3)
#    lfs3_unmount(&lfs3) => 0;
#    #elif defined(LFS2)
#    lfs2_unmount(&lfs2) => 0;
#    #elif defined(SPIFFS)
#    SPIFFS_unmount(&spiffs);
#    free(work_buffer);
#    free(fd_buffer);
#    free(scache_buffer);
#    #elif defined(YAFFS2)
#    yaffs_unmount("/") => 0;
#    yaffs_remove_device(&yaffs2);
#    #endif
#'''
#
## random write throughput
#[cases.bench_mount_random]
#defines.GRANULAR = false
#code = '''
#    #if defined(LFS3)
#    lfs3_t lfs3;
#    lfs3_format(&lfs3, LFS3_F_RDWR, CFG) => 0;
#    lfs3_mount(&lfs3, LFS3_M_RDWR | LFS3_M_MKCONSISTENT, CFG) => 0;
#    #elif defined(LFS2)
#    lfs2_t lfs2;
#    lfs2_format(&lfs2, CFG_LFS2) => 0;
#    lfs2_mount(&lfs2, CFG_LFS2) => 0;
#    lfs2_fs_mkconsistent(&lfs2) => 0;
#    #elif defined(SPIFFS)
#    spiffs spiffs;
#    spiffs.user_data = CFG;
#    uint8_t *work_buffer = malloc(2*SPAGE_SIZE);
#    uint8_t *fd_buffer = malloc(FD_SIZE);
#    uint8_t *scache_buffer = malloc(SCACHE_SIZE);
#    // spiffs format without magic just needs a disk erase
#    SPIFFS_mount(&spiffs, (spiffs_config*)CFG_SPIFFS, work_buffer,
#            fd_buffer, FD_SIZE,
#            scache_buffer, SCACHE_SIZE,
#            NULL) => 0;
#    #elif defined(YAFFS2)
#    // note this implicitly zeros yaffs_dev
#    struct yaffs_dev yaffs2 = {
#        .driver_context = CFG,
#        .param = CFG_YAFFS2->param,
#        .drv = CFG_YAFFS2->drv,
#    };
#    yaffs_add_device(&yaffs2);
#    yaffs_format("/", false, false, false) => 0;
#    yaffs_mount("/") => 0;
#    #endif
#    if (!SKIP_WARMUP) {
#        BENCH_STACK_PAUSE();
#        BENCH_HEAP_PAUSE();
#        #if defined(LFS3)
#        int err = bench_helpers_warmup(CFG, &lfs3);
#        #elif defined(LFS2)
#        int err = bench_helpers_warmup(CFG, &lfs2);
#        #elif defined(SPIFFS)
#        int err = bench_helpers_warmup(CFG, &spiffs);
#        #elif defined(YAFFS2)
#        int err = bench_helpers_warmup(CFG, &yaffs2);
#        #endif
#        if (err) {
#            LFS3_ERROR("Bench warmup failed: %d", err);
#            return;
#        }
#        BENCH_HEAP_RESUME();
#        BENCH_STACK_RESUME();
#    }
#    uint32_t prng = SEED;
#
#    // reset our timer
#    BENCH_SIMRESET();
#
#    // open a file
#    #if defined(LFS3)
#    lfs3_file_t file;
#    lfs3_file_open(&lfs3, &file, "bench_random",
#            LFS3_O_WRONLY | LFS3_O_CREAT | LFS3_O_EXCL
#                | ((GRANULAR) ? LFS3_O_GRANULAR : 0)) => 0;
#    #elif defined(LFS2)
#    lfs2_file_t file;
#    lfs2_file_open(&lfs2, &file, "bench_random",
#            LFS2_O_WRONLY | LFS2_O_CREAT | LFS2_O_EXCL) => 0;
#    #elif defined(SPIFFS)
#    spiffs_file fd = SPIFFS_open(&spiffs, "bench_random",
#            SPIFFS_WRONLY | SPIFFS_CREAT | SPIFFS_EXCL, 0777);
#    assert(fd >= 0);
#    #elif defined(YAFFS2)
#    int fd = yaffs_open("bench_random", O_WRONLY | O_CREAT | O_EXCL, 0777);
#    assert(fd >= 0);
#    #endif
#    lfs3_off_t size = 0;
#    uint64_t written = 0;
#    // ok, one of these needs to be non-zero
#    LFS3_ASSERT(SIM_TIME > 0 || SIM_SIZE > 0);
#    while (!(SIM_SIZE && written >= (uint64_t)SIM_SIZE)
#            && !(SIM_TIME && BENCH_SIMTIME() >= SIM_TIME)) {
#        BENCH_START("write");
#        // seek to a random location
#        lfs3_off_t pos = BENCH_PRNG(&prng) % SIZE;
#        #if defined(LFS3)
#        lfs3_file_seek(&lfs3, &file, pos, LFS3_SEEK_SET) => pos;
#        #elif defined(LFS2)
#        lfs2_file_seek(&lfs2, &file, pos, LFS2_SEEK_SET) => pos;
#        #elif defined(SPIFFS)
#        // if we seek beyond the end of the file we need to emulate zeros
#        if (pos > size) {
#            SPIFFS_lseek(&spiffs, fd, 0, SPIFFS_SEEK_END) => size;
#            for (lfs3_size_t i = 0; i < (pos - size); i++) {
#                SPIFFS_write(&spiffs, fd, &(uint8_t){0}, 1) => 1;
#            }
#        }
#        SPIFFS_lseek(&spiffs, fd, pos, SPIFFS_SEEK_SET) => pos;
#        #elif defined(YAFFS2)
#        yaffs_lseek(fd, pos, SEEK_SET) => pos;
#        #endif
#
#        // write to the file
#        uint8_t wbuf[CHUNK];
#        for (lfs3_size_t j = 0; j < CHUNK; j++) {
#            wbuf[j] = 'a' + (BENCH_PRNG(&prng) % 26);
#        }
#        #if defined(LFS3)
#        lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
#        #elif defined(LFS2)
#        lfs2_file_write(&lfs2, &file, wbuf, CHUNK) => CHUNK;
#        #elif defined(SPIFFS)
#        SPIFFS_write(&spiffs, fd, wbuf, CHUNK) => CHUNK;
#        #elif defined(YAFFS2)
#        yaffs_write(fd, wbuf, CHUNK) => CHUNK;
#        #endif
#
#        size = lfs3_max(size, pos + CHUNK);
#        written += CHUNK;
#        BENCH_STOP("write", written);
#
#        // run gc
#        if (GC) {
#            BENCH_START("gc");
#            bench_ns_t gc_start = BENCH_SIMTIME();
#            #if defined(LFS3)
#            lfs3_gc_t gc;
#            lfs3_gc_open(&lfs3, &gc, GC_FLAGS) => 0;
#            bench_mount_gc_start();
#            while (!GC_TIME
#                    || BENCH_SIMTIME() - gc_start < (bench_ns_t)GC_TIME) {
#                lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
#                assert(steps >= 0 || steps == LFS3_ERR_IO);
#                if (steps == 0) {
#                    break;
#                }
#            }
#            bench_mount_gc_stop();
#            lfs3_gc_close(&lfs3, &gc) => 0;
#            #elif defined(LFS2)
#            bench_mount_gc_start();
#            if (!GC_TIME
#                    || BENCH_SIMTIME() - gc_start < (bench_ns_t)GC_TIME) {
#                int err = lfs2_fs_gc(&lfs2);
#                assert(!err || err == LFS3_ERR_IO);
#            }
#            bench_mount_gc_stop();
#            #elif defined(SPIFFS)
#            bench_mount_gc_start();
#            for (lfs3_block_t i = 0;
#                    i < GC_RETRY_COUNT
#                        && (!GC_TIME
#                            || BENCH_SIMTIME() - gc_start
#                                < (bench_ns_t)GC_TIME);
#                    i++) {
#                int err = SPIFFS_gc(&spiffs, GC_CLEAN_SIZE);
#                assert(!err || err == LFS3_ERR_IO);
#            }
#            bench_mount_gc_stop();
#            #elif defined(YAFFS2)
#            bench_mount_gc_start();
#            if (!GC_TIME
#                    || BENCH_SIMTIME() - gc_start < (bench_ns_t)GC_TIME) {
#                if (!setjmp(bench_mount_gc_jmp)) {
#                    yaffs_do_background_gc("/", 0);
#                }
#            }
#            bench_mount_gc_stop();
#            #endif
#            BENCH_STOP("gc", written);
#        }
#    }
#    #if defined(LFS3)
#    lfs3_file_close(&lfs3, &file) => 0;
#    #elif defined(LFS2)
#    lfs2_file_close(&lfs2, &file) => 0;
#    #elif defined(SPIFFS)
#    SPIFFS_close(&spiffs, fd) => 0;
#    #elif defined(YAFFS2)
#    yaffs_close(fd) => 0;
#    #endif
#
#    // report the total stack/heap usage after the benchmark
#    #ifdef BENCH_STACK
#    BENCH_RESULT("stack", written, BENCH_STACK_WATERMARK());
#    #endif
#    #ifdef BENCH_HEAP
#    BENCH_RESULT("heap", written, BENCH_HEAP_WATERMARK());
#    #endif
#
#    // find the total disk usage after the benchmark
#    #if defined(LFS3)
#    uintmax_t usage = bench_helpers_usage(CFG, &lfs3);
#    #elif defined(LFS2)
#    uintmax_t usage = bench_helpers_usage(CFG, &lfs2);
#    #elif defined(SPIFFS)
#    uintmax_t usage = bench_helpers_usage(CFG, &spiffs);
#    #elif defined(YAFFS2)
#    uintmax_t usage = bench_helpers_usage(CFG, &yaffs2);
#    #endif
#    BENCH_RESULT("usage", written, usage);
#
#    #if defined(LFS3)
#    lfs3_unmount(&lfs3) => 0;
#    #elif defined(LFS2)
#    lfs2_unmount(&lfs2) => 0;
#    #elif defined(SPIFFS)
#    SPIFFS_unmount(&spiffs);
#    free(work_buffer);
#    free(fd_buffer);
#    free(scache_buffer);
#    #elif defined(YAFFS2)
#    yaffs_unmount("/") => 0;
#    yaffs_remove_device(&yaffs2);
#    #endif
#'''

# logging write throughput
#
# two big differences from seq:
# 1. fruncate/rotations instead of rewind+truncate
# 2. sync called on every write
[cases.bench_mount_logging]
defines.FRUNCATE = false
code = '''
    #if defined(LFS3)
    lfs3_t lfs3;
    lfs3_format(&lfs3, LFS3_F_RDWR, CFG) => 0;
    lfs3_mount(&lfs3, LFS3_M_RDWR | LFS3_M_MKCONSISTENT, CFG) => 0;
    #elif defined(LFS2)
    lfs2_t lfs2;
    lfs2_format(&lfs2, CFG_LFS2) => 0;
    lfs2_mount(&lfs2, CFG_LFS2) => 0;
    lfs2_fs_mkconsistent(&lfs2) => 0;
    #elif defined(SPIFFS)
    spiffs spiffs;
    spiffs.user_data = CFG;
    uint8_t *work_buffer = malloc(2*SPAGE_SIZE);
    uint8_t *fd_buffer = malloc(FD_SIZE);
    uint8_t *scache_buffer = malloc(SCACHE_SIZE);
    // spiffs format without magic just needs a disk erase
    SPIFFS_mount(&spiffs, (spiffs_config*)CFG_SPIFFS, work_buffer,
            fd_buffer, FD_SIZE,
            scache_buffer, SCACHE_SIZE,
            NULL) => 0;
    #elif defined(YAFFS2)
    // note this implicitly zeros yaffs_dev
    struct yaffs_dev yaffs2 = {
        .driver_context = CFG,
        .param = CFG_YAFFS2->param,
        .drv = CFG_YAFFS2->drv,
    };
    yaffs_add_device(&yaffs2);
    yaffs_format("/", false, false, false) => 0;
    yaffs_mount("/") => 0;
    #endif
    if (!SKIP_WARMUP) {
        BENCH_STACK_PAUSE();
        BENCH_HEAP_PAUSE();
        #if defined(LFS3)
        int err = bench_helpers_warmup(CFG, &lfs3);
        #elif defined(LFS2)
        int err = bench_helpers_warmup(CFG, &lfs2);
        #elif defined(SPIFFS)
        int err = bench_helpers_warmup(CFG, &spiffs);
        #elif defined(YAFFS2)
        int err = bench_helpers_warmup(CFG, &yaffs2);
        #endif
        if (err) {
            LFS3_ERROR("Bench warmup failed: %d", err);
            return;
        }
        BENCH_HEAP_RESUME();
        BENCH_STACK_RESUME();
    }
    uint32_t prng = SEED;

    // start unmounted
    #if defined(LFS3)
    lfs3_unmount(&lfs3) => 0;
    #elif defined(LFS2)
    lfs2_unmount(&lfs2) => 0;
    #elif defined(SPIFFS)
    SPIFFS_unmount(&spiffs);
    #elif defined(YAFFS2)
    yaffs_unmount("/") => 0;
    yaffs_remove_device(&yaffs2);
    #endif

    // reset our timer
    BENCH_SIMRESET();

    // ok, one of these needs to be non-zero
    LFS3_ASSERT(SIM_COUNT > 0 || SIM_TIME > 0 || SIM_SIZE > 0);
    uint64_t mounts = 0;
    uint64_t written = 0;
    uint64_t grms = 0;
    while (!(SIM_COUNT && mounts >= (uint64_t)SIM_COUNT)
            && !(SIM_SIZE && written >= (uint64_t)SIM_SIZE)
            && !(SIM_TIME && BENCH_SIMTIME() >= SIM_TIME)) {
        // how long to run this loop?
        bench_ns_t run_start = BENCH_SIMTIME();
        bench_ns_t run_stop = run_start
                + RUN_MIN
                + (BENCH_PRNG(&prng) % (RUN_MAX+1 - RUN_MIN));

        // TODO readonly mount?

        // measure mount time
        BENCH_START("mount");
        mounts += 1;
        #if defined(LFS3)
        lfs3_mount(&lfs3, LFS3_M_RDWR, CFG) => 0;
        // keep track of how many grms we've seen
        if (lfs3.grm.queue[0]) {
            grms += 1;
        }
        // include mkconsistent cost for fairness, inconsistent mounts
        // are the same as read-only mounts
        BENCH_START("mkconsistent");
        lfs3_fs_mkconsistent(&lfs3) => 0;
        BENCH_STOP("mkconsistent", mounts);
        #elif defined(LFS2)
        lfs2_mount(&lfs2, CFG_LFS2) => 0;
        // include mkconsistent cost for fairness, inconsistent mounts
        // are the same as read-only mounts
        BENCH_START("mkconsistent");
        lfs2_fs_mkconsistent(&lfs2) => 0;
        BENCH_STOP("mkconsistent", mounts);
        #elif defined(SPIFFS)
        SPIFFS_mount(&spiffs, (spiffs_config*)CFG_SPIFFS, work_buffer,
                fd_buffer, FD_SIZE,
                scache_buffer, SCACHE_SIZE,
                NULL) => 0;
        #elif defined(YAFFS2)
        yaffs_add_device(&yaffs2);
        yaffs_mount("/") => 0;
        #endif
        BENCH_STOP("mount", mounts);

        // open a file
        BENCH_START("open");
        #if defined(LFS3)
        lfs3_file_t file;
        lfs3_file_open(&lfs3, &file, "bench_log",
                LFS3_O_WRONLY | LFS3_O_CREAT | LFS3_O_APPEND) => 0;
        #elif defined(LFS2)
        lfs2_file_t file;
        lfs2_file_open(&lfs2, &file, "bench_log",
                LFS2_O_WRONLY | LFS2_O_CREAT | LFS2_O_APPEND) => 0;
        #elif defined(SPIFFS)
        spiffs_file fd = SPIFFS_open(&spiffs, "bench_log",
                SPIFFS_WRONLY | SPIFFS_CREAT | SPIFFS_APPEND, 0777);
        assert(fd >= 0);
        #elif defined(YAFFS2)
        int fd = yaffs_open("bench_log",
                O_WRONLY | O_CREAT | O_APPEND, 0777);
        assert(fd >= 0);
        #endif
        BENCH_STOP("open", mounts);
        while (!(SIM_COUNT && mounts >= (uint64_t)SIM_COUNT)
                && !(SIM_SIZE && written >= (uint64_t)SIM_SIZE)
                && !(SIM_TIME && BENCH_SIMTIME() >= SIM_TIME)
                // time to reboot?
                && (bench_ns_t)BENCH_SIMTIME() < run_stop) {
            BENCH_START("write");
            // append to log
            uint8_t wbuf[CHUNK];
            for (lfs3_size_t j = 0; j < CHUNK; j++) {
                wbuf[j] = 'a' + (BENCH_PRNG(&prng) % 26);
            }
            #if defined(LFS3)
            lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
            #elif defined(LFS2)
            lfs2_file_write(&lfs2, &file, wbuf, CHUNK) => CHUNK;
            #elif defined(SPIFFS)
            SPIFFS_write(&spiffs, fd, wbuf, CHUNK) => CHUNK;
            #elif defined(YAFFS2)
            yaffs_write(fd, wbuf, CHUNK) => CHUNK;
            #endif

            // sync
            #if defined(LFS3)
            lfs3_file_sync(&lfs3, &file) => 0;
            #elif defined(LFS2)
            lfs2_file_sync(&lfs2, &file) => 0;
            #elif defined(SPIFFS)
            s32_t d = SPIFFS_fflush(&spiffs, fd);
            assert(d >= 0);
            #elif defined(YAFFS2)
            yaffs_fsync(fd) => 0;
            #endif

            // for v3, fruncate, for v2, rotate if full
            //
            // unlike litmus benchmarks we just use SIZE here
            #if defined(LFS3)
            lfs3_soff_t size = lfs3_file_size(&lfs3, &file);
            assert(size >= 0);
            if (FRUNCATE) {
                if (size > SIZE) {
                    lfs3_file_fruncate(&lfs3, &file, SIZE) => 0;
                }
            } else {
                if (size >= SIZE) {
                    lfs3_file_close(&lfs3, &file) => 0;
                    lfs3_rename(&lfs3, "bench_log", "bench_log.1") => 0;
                    lfs3_file_open(&lfs3, &file, "bench_log",
                            LFS3_O_WRONLY
                                | LFS3_O_CREAT
                                | LFS3_O_EXCL
                                | LFS3_O_APPEND) => 0;
                }
            }
            #elif defined(LFS2)
            lfs3_soff_t size = lfs2_file_size(&lfs2, &file);
            assert(size >= 0);
            if (size >= SIZE) {
                lfs2_file_close(&lfs2, &file) => 0;
                lfs2_rename(&lfs2, "bench_log", "bench_log.1") => 0;
                lfs2_file_open(&lfs2, &file, "bench_log",
                        LFS2_O_WRONLY
                            | LFS2_O_CREAT
                            | LFS2_O_EXCL
                            | LFS2_O_APPEND) => 0;
            }
            #elif defined(SPIFFS)
            spiffs_stat stat;
            SPIFFS_fstat(&spiffs, fd, &stat) => 0;
            assert(stat.size >= 0);
            if (stat.size >= SIZE) {
                SPIFFS_close(&spiffs, fd) => 0;
                int err = SPIFFS_remove(&spiffs, "bench_log.1");
                assert(!err || err == SPIFFS_ERR_NOT_FOUND);
                SPIFFS_rename(&spiffs, "bench_log", "bench_log.1") => 0;
                fd = SPIFFS_open(&spiffs, "bench_log",
                        SPIFFS_WRONLY
                            | SPIFFS_CREAT
                            | SPIFFS_EXCL
                            | SPIFFS_APPEND, 0777);
                assert(fd >= 0);
            }
            #elif defined(YAFFS2)
            struct yaffs_stat stat;
            yaffs_fstat(fd, &stat) => 0;
            if (stat.st_size >= SIZE) {
                yaffs_close(fd) => 0;
                yaffs_rename("bench_log", "bench_log.1") => 0;
                fd = yaffs_open("bench_log",
                        O_WRONLY | O_CREAT | O_EXCL | O_APPEND, 0777);
                assert(fd >= 0);
            }
            #endif

            written += CHUNK;
            BENCH_STOP("write", written);
        }
        // close
        BENCH_START("close");
        #if defined(LFS3)
        lfs3_file_close(&lfs3, &file) => 0;
        #elif defined(LFS2)
        lfs2_file_close(&lfs2, &file) => 0;
        #elif defined(SPIFFS)
        SPIFFS_close(&spiffs, fd) => 0;
        #elif defined(YAFFS2)
        yaffs_close(fd) => 0;
        #endif
        BENCH_STOP("close", mounts);

        // unmount
        BENCH_START("unmount");
        #if defined(LFS3)
        lfs3_unmount(&lfs3) => 0;
        #elif defined(LFS2)
        lfs2_unmount(&lfs2) => 0;
        #elif defined(SPIFFS)
        SPIFFS_unmount(&spiffs);
        #elif defined(YAFFS2)
        yaffs_unmount("/") => 0;
        yaffs_remove_device(&yaffs2);
        #endif
        BENCH_STOP("unmount", mounts);
    }

    // report the number of grms
    #ifdef LFS3
    BENCH_RESULT("grms", mounts, grms);
    #endif

    // other cleanup?
    #if defined(LFS3)
    #elif defined(LFS2)
    #elif defined(SPIFFS)
    free(work_buffer);
    free(fd_buffer);
    free(scache_buffer);
    #elif defined(YAFFS2)
    #endif
'''

## many small file write throughput
#[cases.bench_mount_many]
#defines.SET = false
## littlefs2 scales poorly with many + big blocks
#if = 'BENCH_IFDEF_LFS2(BLOCK_SIZE <= 128*1024, true)'
#code = '''
#    #if defined(LFS3)
#    lfs3_t lfs3;
#    lfs3_format(&lfs3, LFS3_F_RDWR, CFG) => 0;
#    lfs3_mount(&lfs3, LFS3_M_RDWR | LFS3_M_MKCONSISTENT, CFG) => 0;
#    #elif defined(LFS2)
#    lfs2_t lfs2;
#    lfs2_format(&lfs2, CFG_LFS2) => 0;
#    lfs2_mount(&lfs2, CFG_LFS2) => 0;
#    lfs2_fs_mkconsistent(&lfs2) => 0;
#    #elif defined(SPIFFS)
#    spiffs spiffs;
#    spiffs.user_data = CFG;
#    uint8_t *work_buffer = malloc(2*SPAGE_SIZE);
#    uint8_t *fd_buffer = malloc(FD_SIZE);
#    uint8_t *scache_buffer = malloc(SCACHE_SIZE);
#    // spiffs format without magic just needs a disk erase
#    SPIFFS_mount(&spiffs, (spiffs_config*)CFG_SPIFFS, work_buffer,
#            fd_buffer, FD_SIZE,
#            scache_buffer, SCACHE_SIZE,
#            NULL) => 0;
#    #elif defined(YAFFS2)
#    // note this implicitly zeros yaffs_dev
#    struct yaffs_dev yaffs2 = {
#        .driver_context = CFG,
#        .param = CFG_YAFFS2->param,
#        .drv = CFG_YAFFS2->drv,
#    };
#    yaffs_add_device(&yaffs2);
#    yaffs_format("/", false, false, false) => 0;
#    yaffs_mount("/") => 0;
#    #endif
#    if (!SKIP_WARMUP) {
#        BENCH_STACK_PAUSE();
#        BENCH_HEAP_PAUSE();
#        #if defined(LFS3)
#        int err = bench_helpers_warmup(CFG, &lfs3);
#        #elif defined(LFS2)
#        int err = bench_helpers_warmup(CFG, &lfs2);
#        #elif defined(SPIFFS)
#        int err = bench_helpers_warmup(CFG, &spiffs);
#        #elif defined(YAFFS2)
#        int err = bench_helpers_warmup(CFG, &yaffs2);
#        #endif
#        if (err) {
#            LFS3_ERROR("Bench warmup failed: %d", err);
#            return;
#        }
#        BENCH_HEAP_RESUME();
#        BENCH_STACK_RESUME();
#    }
#    uint32_t prng = SEED;
#
#    // reset our timer
#    BENCH_SIMRESET();
#
#    uint64_t written = 0;
#    // ok, one of these needs to be non-zero
#    LFS3_ASSERT(SIM_TIME > 0 || SIM_SIZE > 0);
#    while (!(SIM_SIZE && written >= (uint64_t)SIM_SIZE)
#            && !(SIM_TIME && BENCH_SIMTIME() >= SIM_TIME)) {
#        BENCH_START("write");
#        // choose a random filename
#        lfs3_off_t pos = BENCH_PRNG(&prng) % ((SIZE+(CHUNK-1))/CHUNK);
#        char name[256];
#        sprintf(name, "bench_%08x", pos);
#        uint8_t wbuf[CHUNK];
#        memset(wbuf, 'a'+(BENCH_PRNG(&prng) % 26), CHUNK);
#
#        // create the file
#        //
#        // note file == CHUNK here, the sum of all files should add up
#        // roughly to the benchmark SIZE
#        #if defined(LFS3)
#        if (SET) {
#            lfs3_set(&lfs3, name, wbuf, CHUNK) => 0;
#        } else {
#            lfs3_file_t file;
#            lfs3_file_open(&lfs3, &file, name,
#                    LFS3_O_WRONLY | LFS3_O_CREAT | LFS3_O_TRUNC) => 0;
#            lfs3_file_write(&lfs3, &file, wbuf, CHUNK) => CHUNK;
#            lfs3_file_close(&lfs3, &file) => 0;
#        }
#        #elif defined(LFS2)
#        lfs2_file_t file;
#        int err = lfs2_file_open(&lfs2, &file, name,
#                LFS2_O_WRONLY | LFS2_O_CREAT | LFS2_O_TRUNC);
#        // TODO littlefs2 bug? apparently littlefs2 is very fragile with
#        // many files, just log this error and mark the test as bad for now
#        assert(!err || err == LFS2_ERR_NOENT);
#        if (err) {
#            LFS3_ERROR("lfs2 open failure? %d", err);
#            __builtin_trap();
#            return;
#        }
#        lfs2_file_write(&lfs2, &file, wbuf, CHUNK) => CHUNK;
#        err = lfs2_file_close(&lfs2, &file);
#        // TODO littlefs2 bug?
#        assert(!err || err == LFS2_ERR_NOENT);
#        if (err) {
#            LFS3_ERROR("lfs2 close failure? %d", err);
#            __builtin_trap();
#            return;
#        }
#        #elif defined(SPIFFS)
#        spiffs_file fd = SPIFFS_open(&spiffs, name,
#                SPIFFS_WRONLY | SPIFFS_CREAT | SPIFFS_TRUNC, 0777);
#        assert(fd >= 0);
#        SPIFFS_write(&spiffs, fd, wbuf, CHUNK) => CHUNK;
#        SPIFFS_close(&spiffs, fd) => 0;
#        #elif defined(YAFFS2)
#        int fd = yaffs_open(name, O_WRONLY | O_CREAT | O_TRUNC, 0777);
#        assert(fd >= 0);
#        yaffs_write(fd, wbuf, CHUNK) => CHUNK;
#        yaffs_close(fd) => 0;
#        #endif
#
#        written += CHUNK;
#        BENCH_STOP("write", written);
#
#        // run gc
#        if (GC) {
#            BENCH_START("gc");
#            bench_ns_t gc_start = BENCH_SIMTIME();
#            #if defined(LFS3)
#            lfs3_gc_t gc;
#            lfs3_gc_open(&lfs3, &gc, GC_FLAGS) => 0;
#            bench_mount_gc_start();
#            while (!GC_TIME
#                    || BENCH_SIMTIME() - gc_start < (bench_ns_t)GC_TIME) {
#                lfs3_sblock_t steps = lfs3_gc_write(&lfs3, &gc, 1);
#                assert(steps >= 0 || steps == LFS3_ERR_IO);
#                if (steps == 0) {
#                    break;
#                }
#            }
#            bench_mount_gc_stop();
#            lfs3_gc_close(&lfs3, &gc) => 0;
#            #elif defined(LFS2)
#            bench_mount_gc_start();
#            if (!GC_TIME
#                    || BENCH_SIMTIME() - gc_start < (bench_ns_t)GC_TIME) {
#                int err = lfs2_fs_gc(&lfs2);
#                assert(!err || err == LFS3_ERR_IO);
#            }
#            bench_mount_gc_stop();
#            #elif defined(SPIFFS)
#            bench_mount_gc_start();
#            for (lfs3_block_t i = 0;
#                    i < GC_RETRY_COUNT
#                        && (!GC_TIME
#                            || BENCH_SIMTIME() - gc_start
#                                < (bench_ns_t)GC_TIME);
#                    i++) {
#                int err = SPIFFS_gc(&spiffs, GC_CLEAN_SIZE);
#                assert(!err || err == LFS3_ERR_IO);
#            }
#            bench_mount_gc_stop();
#            #elif defined(YAFFS2)
#            bench_mount_gc_start();
#            if (!GC_TIME
#                    || BENCH_SIMTIME() - gc_start < (bench_ns_t)GC_TIME) {
#                if (!setjmp(bench_mount_gc_jmp)) {
#                    yaffs_do_background_gc("/", 0);
#                }
#            }
#            bench_mount_gc_stop();
#            #endif
#            BENCH_STOP("gc", written);
#        }
#    }
#
#    // report the total stack/heap usage after the benchmark
#    #ifdef BENCH_STACK
#    BENCH_RESULT("stack", written, BENCH_STACK_WATERMARK());
#    #endif
#    #ifdef BENCH_HEAP
#    BENCH_RESULT("heap", written, BENCH_HEAP_WATERMARK());
#    #endif
#
#    // find the total disk usage after the benchmark
#    #if defined(LFS3)
#    uintmax_t usage = bench_helpers_usage(CFG, &lfs3);
#    #elif defined(LFS2)
#    uintmax_t usage = bench_helpers_usage(CFG, &lfs2);
#    #elif defined(SPIFFS)
#    uintmax_t usage = bench_helpers_usage(CFG, &spiffs);
#    #elif defined(YAFFS2)
#    uintmax_t usage = bench_helpers_usage(CFG, &yaffs2);
#    #endif
#    BENCH_RESULT("usage", written, usage);
#
#    #if defined(LFS3)
#    lfs3_unmount(&lfs3) => 0;
#    #elif defined(LFS2)
#    lfs2_unmount(&lfs2) => 0;
#    #elif defined(SPIFFS)
#    SPIFFS_unmount(&spiffs);
#    free(work_buffer);
#    free(fd_buffer);
#    free(scache_buffer);
#    #elif defined(YAFFS2)
#    yaffs_unmount("/") => 0;
#    yaffs_remove_device(&yaffs2);
#    #endif
#'''
